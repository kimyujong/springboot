name: Deploy Spring Boot

# [이 부분이 빠져서 에러가 난 겁니다!]
on:
  push:
    branches: [ "main" ]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. JDK Setup
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3. Build
      - name: Build with Gradle
        run: |
          chmod +x gradlew
          ./gradlew clean build -x test

      # 4. [핵심] 서버 중지 및 파일 강제 삭제 (SSH)
      - name: Stop Server & Delete JAR
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🛑 Spring Boot 중지..."
            # PM2 중지 (에러 무시)
            pm2 stop springboot-server || true
            
            # 혹시 안 죽었으면 강제 종료 (포트 8080)
            fuser -k -9 8080/tcp || true
            
            # 파일 삭제 (이제 잡고 있는 프로세스가 없으니 지워짐)
            echo "🗑️ 기존 JAR 삭제..."
            sudo rm -rf /home/ubuntu/springboot/build/libs/*.jar
            
            # [추가] 폴더 소유권을 다시 ubuntu로 돌려놓기 (전송받을 때 에러 방지)
            sudo mkdir -p /home/ubuntu/springboot/build/libs
            sudo chown -R ubuntu:ubuntu /home/ubuntu/springboot

      # 5. 파일 전송 (이제 방해꾼 없음)
      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "build/libs/*.jar"
          target: "/home/ubuntu/springboot"
          rm: false # 위에서 지웠으니 false

      # 6. 서버 다시 시작
      - name: Start Spring Boot
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🚀 Spring Boot 시작..."
            chmod +x /home/ubuntu/springboot/build/libs/*.jar
            pm2 start springboot-server
            pm2 save
            echo "✅ 배포 완료!"
